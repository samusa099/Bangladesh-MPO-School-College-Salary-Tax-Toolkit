# Local Git Workflow

Use this workflow for larger release assets.

```bash
git status
git pull origin main
git add .
git commit -m "release: add v0.1.2 workbook and assets"
git push origin main
```

## Before pushing

- confirm file paths;
- confirm no personal data;
- confirm workbook opens;
- confirm preview images render.
