# Scripts

This folder contains project-specific automation for workbook validation, CSV export, metadata generation, release packaging, and repository quality checks.

Scripts must be reproducible, non-destructive by default, and limited to the Bangladesh MPO School & College Salary Tax Toolkit. Generated outputs should be written to the appropriate `data/`, `metadata/`, `release/`, or `packages/` destination.
